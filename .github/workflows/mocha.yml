name: main action
on:
  push:
    branches:
    - main

jobs:
  mocha:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Install dependencies
        run: npm install
        
      - name: Mocha test
        run: npm run test
